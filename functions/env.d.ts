/// <reference types="@cloudflare/workers-types" />

// Scoped to the Functions: the admin API runs on the Workers runtime, not in a
// browser or in Node, so PagesFunction, D1Database and friends come from here.
export {};
